#include <iostream>
#include <vector>
#include <string>
#include <cmath>
using namespace std;

int main(void){

    cin.tie(0);
    ios::sync_with_stdio(false);

    int n,x;

    //vector<int> s(n);
    int s;

    int tmp;

    for(int i=0;i<n;i++){
        cin >> s;
        tmp = x[bmod]y;
    }


    return 0;
}